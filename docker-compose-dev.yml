version: '3'
services:
  web:
    build:
      context: ./NovskyProject
    environment:
      - SECRET_KEY=testkey
  nginx:
    depends_on:
      - web
    build:
      context: ./nginx
    ports:
      - '4005:80'

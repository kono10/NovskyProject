version: '3'
services:
  web:
    image: "novskytech/novskyproject"
    ports:
      - '8001:8000'
    hostname: web
    environment:
      - SECRET_KEY
  nginx:
    depends_on:
      - web
    image: "novskytech/novskyprojectnginx"
    hostname: nginx
    ports:
      - '80:80'
